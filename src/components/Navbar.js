import navbarStyles from "../styles/Navbar.module.css";

function Navbar() {
  return (
    <nav className={navbarStyles.navbar}>
      <h1 className={navbarStyles.h1}>Navbar</h1>
    </nav>
  );
}

export default Navbar;
